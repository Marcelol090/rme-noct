# M021 - Roadmap

## S01 - SPR compressed payload reader

Read raw compressed SPR payload bytes from frame-table archive offsets.

Stop when a tested Python payload reader returns explicit empty outcomes or `SprCompressedPayload` records from SPR bytes/path without decoding RLE pixels or touching renderer paint paths.
