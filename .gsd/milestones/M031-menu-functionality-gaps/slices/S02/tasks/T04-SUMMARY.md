# T04 Summary - Deferred Backend Reasons

- Import Map: deferred until a core map merge/import backend defines offset,
  house, and spawn policy.
- Import Monsters/NPC: deferred until an OT monster/NPC XML creature-type
  catalog import backend exists.
- Export Minimap: deferred until a real minimap image renderer/exporter exists;
  legacy source is inconsistent.
- Export Tilesets: deferred until a materials/tileset catalog exporter
  equivalent exists.
- Reload Data Files: deferred until the shell exposes a selected client data
  root seam for client asset discovery.
- Missing Items Report: deferred until a version-manager missing item report
  backend exists.
