# Decisions Register

<!-- Append-only. Never edit or remove existing rows. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| 1 | 2026-04-09 | Legacy parity | Legacy menu contract source | `remeres-map-editor-redux/data/menubar.xml` is the authoritative source for menu order, labels, shortcuts, and submenu placement during shell parity work. | Prevents the Python shell from drifting into a second product taxonomy and keeps parity tests objective. | No | Codex |
| 2 | 2026-04-09 | Legacy parity | M5 reuse boundary | Reuse `M5-SHELL-NAVIGATION` only as shell-state and `QSettings` infrastructure, not as proof of full legacy parity. | Keeps GSD history honest and makes each new slice claim only its real delta. | Yes | Codex |
| 3 | 2026-04-09 | Legacy parity | Next slice after Navigate/Window | After `LEGACY-00-CONTRACT`, `LEGACY-90-NAVIGATE`, and `LEGACY-100-WINDOW`, the next active slice is `LEGACY-30-EDITOR`. | Matches the design spec execution order and focuses the next step on the highest-value unresolved legacy menu group. | Yes | Codex |
| 4 | 2026-04-16 | Item palette perf | Perf audit thresholding | Measure `ItemPaletteWidget` perf with direct API timing and explicit thresholds for load, search, category switch, and cache hit; do not include paint or event-loop time in the acceptance gate. | Keeps T15 honest, repeatable, and resistant to refactor creep. | Yes | Codex |
| 5 | 2026-04-18 | Canvas renderer | Production host surface | Use `QOpenGLWidget` as the production default canvas host, keep `PlaceholderCanvasWidget` only for explicit fallback/test injection, and keep offscreen CI honest by not requiring a valid GL context for seam verification. | Matches the legacy redux architecture more closely, avoids another future widget swap, and keeps the CI contract realistic under `QT_QPA_PLATFORM=offscreen`. | Yes | Codex |
| 6 | 2026-04-18 | Canvas renderer | Viewport owner boundary | Store center position, scroll origin, floor, zoom, and per-view snapshot/restore in `EditorViewport`, with `MainWindow` keeping temporary compatibility mirrors only. | Moves R042 out of ad hoc shell fields while keeping R043 screen/map translation isolated for the next slice. | Yes | Codex |
| 7 | 2026-04-18 | Canvas renderer | Screen/map math location | Keep legacy-style `screen_to_map`, `map_to_screen`, and visible-rect math on `EditorViewport`, and have canvas input consume viewport snapshots instead of duplicating coordinate math in `MainWindow`. | Matches the legacy `MainMapViewMath` separation and gives the later renderer draw path a single camera math authority. | Yes | Codex |
| 8 | 2026-04-18 | Canvas renderer | Shell snapshot boundary | Keep `ShellStateSnapshot` limited to shell flags; keep center, floor, zoom, and previous-position history on the per-view `EditorViewport`. | Prevents duplicated camera state after S02 and makes tab switching restore one viewport owner instead of two partially overlapping snapshots. | Yes | Codex |
| 9 | 2026-04-18 | Renderer draw foundation | Frame plan before sprite drawing | Build a pure frame plan from `MapModel` and `EditorViewport.visible_rect()` before adding sprite atlas or OpenGL tile drawing. | Matches the legacy MapDrawer separation while keeping the first renderer step testable under offscreen CI. | Yes | Codex |
| 10 | 2026-04-18 | Renderer draw foundation | Diagnostic primitives before sprite atlas | Project frame-plan tile commands into simple screen-space diagnostic rectangles before wiring real sprite artwork. | Gives the renderer host a verified drawable surface while avoiding fake legacy sprite parity. | Yes | Codex |
| 11 | 2026-04-18 | Workflow health | Integrated Codex stack is valid without legacy local agents | Treat local `planner/worker/docs_researcher/reviewer` TOML files and standalone `c7-mcp-server` as optional when the active Codex session already exposes agents, skills, and Context7 MCP. | Prevents `npm run preflight` from failing on stale local-agent assumptions while still validating Caveman, Superpowers, AGENTS.md, and Ollama availability. | Yes | Codex |
